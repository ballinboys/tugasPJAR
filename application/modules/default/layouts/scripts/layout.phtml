<?php $userWeb = $_SESSION["user_web"] ?? null; ?>

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no" />
    <title>
        <?php
        if ($this->title) {
            echo $this->escape($this->title) . " - Network Lab";
        } else {
            echo "Network Lab";
        }
        ?>
    </title>
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@300;400;600;700&display=swap" rel="stylesheet" />
    <link href="<?= $this->baseUrl('/assets/css/styles.min.css'); ?>" rel="stylesheet" />
    <link href="<?= $this->baseUrl('/assets/css/custom.css'); ?>" rel="stylesheet" />
    <script src="<?= $this->baseUrl('/assets/js/custom.js'); ?>"></script>
    <script src="<?= $this->baseUrl('/assets/js/app.min.js'); ?>"></script>
</head>

<body>
    <nav class="navbar-custom navbar navbar-expand-lg">
        <div class="container-fluid d-flex align-items-center justify-content-between">
            <a class="navbar-brand d-flex align-items-center gap-2 mb-0" href="/">
                <span class="brand-mark">
                    <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <circle cx="5" cy="5" r="2.5" fill="currentColor" />
                        <circle cx="19" cy="5" r="2.5" fill="currentColor" />
                        <circle cx="12" cy="12" r="2.5" fill="currentColor" />
                        <circle cx="5" cy="19" r="2.5" fill="currentColor" />
                        <circle cx="19" cy="19" r="2.5" fill="currentColor" />
                        <path d="M5 5L12 12M19 5L12 12M5 19L12 12M19 19L12 12" stroke="currentColor" stroke-width="1.4" />
                    </svg>
                </span>
                <span class="brand-text d-flex flex-column">
                    <span class="brand-title">Network Lab</span>
                    <span class="brand-sub d-none d-sm-inline">Pemrograman Jaringan</span>
                </span>
            </a>
            <?php if ($userWeb): ?>
                <div class="user-chip">
                    <span class="user-avatar">
                        <?= $this->escape(mb_strtoupper(mb_substr((string) ($userWeb["name"] ?? '?'), 0, 1))) ?>
                    </span>
                    <span class="d-none d-sm-flex flex-column">
                        <span class="user-name fw-bold"><?= $this->escape($userWeb["name"] ?? '') ?></span>
                        <span class="user-email"><?= $this->escape($userWeb["email"] ?? '') ?></span>
                    </span>
                    <a href="/index/logout" class="btn-icon-only" title="Keluar">
                        <img src="<?= $this->baseUrl('/assets/img/ic-logout-sm.svg'); ?>" width="18" height="18" alt="Keluar" />
                    </a>
                </div>
            <?php endif; ?>
        </div>
    </nav>

    <main class="main bg-soft-white">
        <div class="content">
            <?php echo $this->layout()->content; ?>
        </div>
    </main>

    <footer class="site-footer">
        Network Lab — Frontend (sesi &amp; UI) berkomunikasi dengan Backend API melalui HTTP + tanda tangan HMAC.
    </footer>

    <div class="modal fade" id="modalError" data-bs-backdrop="static" tabindex="-1" aria-labelledby="modalErrorLabel">
        <div class="modal-dialog modal-dialog-centered modal-fit">
            <div class="modal-content">
                <div class="modal-body px-5 py-4">
                    <div class="d-flex flex-column align-items-center gap-4">
                        <div class="d-flex align-items-center py-2 px-4 mx-3">
                            <img src="<?= $this->baseUrl('/assets/img/ic-toast-error.svg'); ?>" width="67px"
                                height="67px" alt="icon failed" />
                        </div>
                        <div class="d-flex flex-column align-items-center gap-2">
                            <h3 id="modalFailedTitle" class="text-neutral-100 fs-7 fw-bold mb-0 text-center">
                                <?= $this->escape($_SESSION["modalError"] ?? '') ?>
                            </h3>
                            <p class="text-neutral-70 fs-8 mb-0 text-center">
                                <?php // Upload validator messages embed the user's filename — escape
                                echo $this->escape($_SESSION["modalErrorMessage"] ?? ''); ?>
                            </p>
                        </div>
                    </div>
                </div>
                <div class="modal-footer border-0">
                    <button class="btn btn-primary w-100" data-bs-toggle="modal" data-bs-target="#modalError">
                        Ok
                    </button>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="modalAlert" data-bs-backdrop="static" tabindex="-1" aria-labelledby="modalAlertLabel">
        <div class="modal-dialog modal-dialog-centered modal-fit">
            <div class="modal-content">
                <div class="modal-body px-5 py-4">
                    <div class="d-flex flex-column align-items-center gap-4">
                        <div class="d-flex align-items-center py-2 px-4 mx-3">
                            <img src="<?= $this->baseUrl('/assets/img/ic-toast-warning.svg'); ?>" width="67px"
                                height="67px" alt="icon alert" />
                        </div>
                        <div class="d-flex flex-column align-items-center gap-2">
                            <h3 id="modalAlertTitle" class="text-neutral-100 fs-7 fw-bold mb-0 text-center">
                                <?= $this->escape($_SESSION["modalAlert"] ?? '') ?>
                            </h3>
                            <p class="text-neutral-70 fs-8 mb-0 text-center">
                                <?= $this->escape($_SESSION["modalAlertMessage"] ?? '') ?>
                            </p>
                        </div>
                    </div>
                </div>
                <div class="modal-footer border-0">
                    <button class="btn btn-primary w-100" data-bs-toggle="modal" data-bs-target="#modalAlert">
                        Ok
                    </button>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="modalLoading" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1"
        aria-labelledby="modalLoadingLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-fit">
            <div class="modal-content">
                <div class="modal-body">
                    <div class="d-flex flex-column align-items-center gap-4 px-2 pt-2">
                        <span class="spinner-progress"></span>
                        <h3 id="modalLoadingTitle" class="text-neutral-100 fs-7 fw-bold mb-0">
                            Memproses...
                        </h3>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script nonce="<?= $_SESSION['cspNonce'] ?>" type="text/javascript">
        $(function () {
            <?php if (isset($_SESSION["modalError"])): ?>
                let modalError = new bootstrap.Modal(
                    document.querySelector("#modalError")
                );

                modalError.show();
                <?php
                unset($_SESSION["modalError"]);
                unset($_SESSION["modalErrorMessage"]);
            endif; ?>

            <?php if (isset($_SESSION["modalAlert"])): ?>
                let modalAlert = new bootstrap.Modal(
                    document.querySelector("#modalAlert")
                );

                modalAlert.show();
                <?php
                unset($_SESSION["modalAlert"]);
                unset($_SESSION["modalAlertMessage"]);
            endif; ?>
        });
    </script>
</body>

</html>
